# 开发 / 测试依赖
# 安装：pip install -r requirements.txt -r requirements-dev.txt

# ─────────────── 测试 ───────────────
pytest~=8.0.0
pytest-asyncio~=0.23.0
pytest-cov~=4.1.0
pytest-mock~=3.12.0
pytest-timeout~=2.3.0     # 防止 CI hang
respx~=0.21.0             # HTTP mock（mock akshare/serpapi 等）

# ─────────────── Lint / 类型 ───────────────
ruff~=0.3.0
mypy~=1.8.0

# ─────────────── 杂项 ───────────────
ipython~=8.20.0
