# AI Agent 后端运行依赖（生产）
# 锁定 minor 版本：~= 保证 bug fix 自动收，但避免 major 突然破坏。
# 实际部署时用 `pip freeze > requirements.lock` 锁定完全版本。

# ─────────────── Web framework ───────────────
fastapi~=0.110.0
uvicorn[standard]~=0.27.0
python-multipart~=0.0.9       # /api/upload 需要

# ─────────────── LangChain / LangGraph ───────────────
langchain~=1.0.0
langchain-openai~=0.3.0
langchain-chroma~=0.2.0
langchain-text-splitters~=0.3.0
langchain-community~=0.3.0
langgraph~=0.3.0
langgraph-checkpoint~=2.0.0
langgraph-checkpoint-sqlite~=2.0.0
chromadb~=0.5.0

# ─────────────── 数据科学 / 工具 ───────────────
numpy~=1.26.0
pandas~=2.1.0
matplotlib~=3.7.0
requests~=2.31.0
httpx~=0.27.0
sse-starlette~=2.0.0

# ─────────────── 第三方集成 ───────────────
serpapi~=0.1.0
akshare~=1.10.0
python-dotenv~=1.0.0

# ─────────────── MCP (Model Context Protocol) ───────────────
mcp~=1.0.0
psutil~=5.9.0
jmespath~=1.0.0
